<h1>{{categoria.nome}}</h1>

{{#each postagens}}
    <div class="card mt-4">
        <div class="card-body">
            <h3>{{titulo}}</h3>
            <small>{{descricao}}</small><br>
            <a href="/postagem/{{slug}}"><button class="btn btn-success">Leia mais</button></a>
            <hr>
            <small>Data de Criação: {{data}}</small>
        </div>
    </div>
{{else}}
    <p>Nenhuma potagem registrada!</p>
{{/each}}